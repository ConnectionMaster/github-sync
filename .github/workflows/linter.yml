name: Linter

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - name: Run Shellcheck
        uses: azohra/shell-linter@latest
